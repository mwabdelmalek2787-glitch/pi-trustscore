# Public publishable values used by the Vite frontend.
# Copy to .env.local and fill values. Do NOT put server-side secrets here
# (anything prefixed with VITE_ is bundled and visible to all users).
VITE_PI_APP_ID=
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
